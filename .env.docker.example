# Docker Environment Configuration for 浮生十梦
# Copy this file to .env and configure your settings

# === OpenAI API Settings ===
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
OPENAI_MODEL_CHEAT_CHECK=gpt-3.5-turbo

# === JWT Settings ===
# Generate a secure secret key with: openssl rand -hex 32
SECRET_KEY=your_very_secure_secret_key_here_change_this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=600

# === Authentication Settings (Username/Password System) ===
# Format: username1:password1,username2:password2,username3:password3
# Example: AUTH_USERS=admin:admin123,player1:password1,player2:password2
AUTH_USERS=admin:changeme123,demo:demo123

# === Server Settings ===
# Port on host machine to expose the application
HOST_PORT=8000

# === Nginx Settings (for domain support) ===
# Uncomment and configure if using nginx profile
# NGINX_HTTP_PORT=80
# NGINX_HTTPS_PORT=443

# === Domain Configuration ===
# If you want to use a domain name, set it here
# DOMAIN_NAME=your-domain.com

# === SSL Configuration ===
# Set to true if you have SSL certificates in nginx/ssl/
# SSL_ENABLED=false

# === Backup Settings ===
# Directory for automatic backups (optional)
# BACKUP_DIR=./backups

# === Logging Settings ===
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# === Game Settings ===
# Maximum number of concurrent players (optional)
# MAX_CONCURRENT_PLAYERS=100

# === Database Settings ===
# Database will be stored in ./data/veloera.db by default
# No additional configuration needed for SQLite
